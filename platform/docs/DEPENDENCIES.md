# Dependency and image evidence

Python and JavaScript packages are exact-pinned in `backend/uv.lock` and `frontend/package-lock.json` once generated by their official lock tools. Compose image tags are exact release tags. Image content digests have not been recorded because the images have not been pulled in this controlled environment; production release is blocked until CI stores verified registry digests and the Compose file uses them.
