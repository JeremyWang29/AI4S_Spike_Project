# V0.13 引导式范围与回传反馈验收（2026-09-23）

验收目标：已确认的 Q1–Q12 范围；隔离 SQLite 浏览器项目和 Docker PostgreSQL 测试库。页面项目题目采用 PSMB5/LSD1-PRMT5/铁死亡/食管鳞癌示例。以下两条页面回传题录与自动化用的 24 条题录均为合成数据，只用于功能验收，不能作为科学证据或正式 Gold。

| 检查 | 结果 |
| --- | --- |
| Django `check` 与迁移检查 | 通过；没有遗漏迁移 |
| SQLite 后端完整测试 | 65 个运行通过，5 个既有条件跳过 |
| Docker PostgreSQL 后端完整测试 | 65 个运行通过；测试库自动清理 |
| 前端测试与生产构建 | 29 个测试通过，构建成功 |
| 页面登录与范围 | 隔离账号登录；五个字段可选择“探索中”；语言可多选；首轮纳排为空仍可保存并确认；确认后范围历史只读 |
| 页面回传和诊断 | 导入 2 条题录；固定样本显示实际 2 条、缺 18 条；人工标注一条相关、一条不相关；按反馈提出纳排草案 |
| 页面影响预览和确认 | 排除词命中已标相关题录时显示 1 条；修改排除词并再次预览显示 0 条；显式确认后范围从 V1 升为 V2，原记录保留 |
| 自动化回传边界 | 24 条导入、跨平台重复去重、首样本 20 条与追加 4 条不重复、过期影响预览拒绝、原始记录及历史保留 |
| 分层抽样与类型词 | 两个任务各 15 条的总体按同一种子重复抽样均得到各 10 条；同义词并入父块，上位词与相关词形成单独变体任务 |
| 模型建议契约 | 服务端模拟提供方返回 3 项/字段及类型化同义词；同输入复用收据；未授权拒绝外发。未配置真实提供方，无真实建议质量结论 |

剩余部署条件：配置经过许可的模型服务、验证真实建议质量和知识图谱数据源；平台检索式编译及正式 Gold 查全查准率验收仍需其独立阶段，诊断 20 条不能替代。
